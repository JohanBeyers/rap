@EndUserText.label: 'access control for travels 00'
@MappingRole: true
define role Z00_C_TRAVEL {
    grant
        select
            on
                Z00_C_TRAVEL
                    where
                        inheriting conditions from entity Z00_R_TRAVEL;
                        
}